package dzh.com.JavaSE.com.characteristic.method;

/**
 * 谨慎选择方法名。方法的命名应该始终遵守命名规范，要易于理解且符合该方法的功能。
 * 不要过于追求提供便利的方法。每个方法应该尽其所能。方法太多会使类难以学习。
 * 避免过长的参数列表。应该在4个或以下。
 * 对于参数类型，要优先使用接口而不是类。如优先使用List作为参数，而不是ArrayList。
 * 对于boolean参数，要优先使用两个元素的枚举类型。
 *
 * 返回0长度的数组或者集合，而不是null
 */
public class MethodDesignSkill {
}
